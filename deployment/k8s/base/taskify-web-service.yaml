apiVersion: v1
kind: Service
metadata:
  name: taskify-web
  namespace: taskify
spec:
  selector:
    app: taskify-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
