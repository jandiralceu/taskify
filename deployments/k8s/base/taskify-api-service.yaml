apiVersion: v1
kind: Service
metadata:
  name: taskify-api
  namespace: taskify
spec:
  selector:
    app: taskify-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
