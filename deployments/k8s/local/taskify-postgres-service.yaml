apiVersion: v1
kind: Service
metadata:
  name: taskify-postgres
  namespace: taskify
spec:
  selector:
    app: taskify-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
